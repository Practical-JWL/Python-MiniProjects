# 🎳 볼링 300 시뮬레이터 실습 노트

## 📝 학습 내용 및 규칙 정리
본 프로젝트는 파이썬 Flet을 활용하여 볼링의 점수 체계와 UI 비동기 처리를 학습하기 위해 제작되었습니다.

### 1. 볼링 점수 규칙 (Logic)
* **스트라이크(Strike)**: 첫 투구에 10핀 모두 처리 (다음 2구 점수 합산)
* **스페어(Spare)**: 두 번의 투구로 10핀 처리 (다음 1구 점수 합산)
* **10프레임**: 보너스 투구 규칙 적용 (최대 3구)

### 2. 기술적 과제 해결 (Troubleshooting)
* **문제점**: `AlertDialog`(팝업) 사용 시 메인 스레드 동기화 문제로 UI가 멈추는(Freezing) 현상 발생.
* **해결책**: 팝업 방식 대신 **'화면 전환(View Switching)'** 방식을 도입하여 `self.controls`를 직접 교체.
* **성과**: 메모리 점유율을 낮추고, 어떤 사양에서도 끊김 없는 사용자 경험(UX)을 구현함.

> [!TIP]
> 복잡한 UI를 다룰 때는 팝업보다 상태(State)에 따른 화면 전환이 안정성 면에서 유리함을 학습함.

---

## 볼링게임 ##
---
🎳 볼링 300 시뮬레이터: 코드 로직 및 구조 정리
본 문서는 **Flet(Python UI Framework)**을 활용하여 제작된 멀티플레이어 볼링 시뮬레이터의 핵심 로직과 코드 실행 순서를 정리한 가이드입니다.

### 1. 볼링 게임 핵심 규칙 (Domain Logic) ###
코드 내에 구현된 볼링 점수 계산 및 진행 규칙임.

📋 프레임 및 투구 규칙
- 기본 구성: 총 10프레임으로 진행됨.

- 일반 프레임 (1~9프레임):

- 최대 2번의 투구 기회가 주어짐.

- 스트라이크(X): 첫 투구에 10핀을 모두 쓰러뜨리면 해당 프레임 종료.

- 스페어(/): 두 번째 투구까지 합쳐서 10핀을 쓰러뜨린 경우.

- 마지막 프레임 (10프레임):

        - 스트라이크나 스페어를 기록하면 최대 3번까지 투구가 가능함.

🔢 점수 계산 로직 (calculate_total_score)
- 볼링은 투구 시점보다 나중에 점수가 확정되는 '누적 합산' 방식임.

        - 스트라이크: 10점 + 다음 2번의 투구 점수 합산.

        - 스페어: 10점 + 다음 1번의 투구 점수 합산.

        - 오픈: 스트라이크/스페어 실패 시 해당 프레임에서 쓰러뜨린 핀 수만 합산.

---
### 2. 데이터 구조: 플레이어 객체 (create_player_dict) ###
각 플레이어의 상태는 Python 딕셔너리로 관리되어 멀티플레이 환경을 지원합니다.

```python
{
    "name": "플레이어 이름",
    "is_bot": True/False,      # 봇 여부
    "rolls": [],               # 모든 투구의 쓰러진 핀 수 기록 (리스트)
    "current_frame": 1,        # 현재 진행 중인 프레임 (1-10)
    "current_roll": 1,         # 현재 프레임 내 투구 차례 (1-3)
    "pins": [1, 2, ..., 10],   # 현재 남은 핀 번호 목록
    "game_over": False         # 개인 게임 종료 여부
}
```

---

### 3. 코드 실행 흐름 (Execution Flow) ###
Step 1: 초기화 (__init__)
- 드롭다운 메뉴(사람/봇 수 설정) 초기화.

- 상태 변수(일시정지 여부, 타이머 등) 기본값 설정.

- show_setup_screen 호출을 통해 초기 설정 화면 출력.

Step 2: 게임 시작 (start_game → init_game_state)
- 설정된 인원만큼 플레이어 딕셔너리를 생성하여 리스트(self.players)에 저장.

- active_idx = 0으로 첫 번째 순서를 지정하고 게임 화면(show_game_screen)으로 전환.

Step 3: 게임 루프 (execute_roll → process_roll_logic)
- 투구 실행: random.randint를 통해 남은 핀 중 무작위로 쓰러뜨림.

- 로직 처리: process_roll_logic에서 현재 투구 결과에 따라 프레임을 넘길지, 추가 투구를 할지 결정.

- UI 업데이트: 점수판(update_game_ui)과 핀 그래픽(🔴/⚪) 갱신.

- 턴 전환: 투구가 끝나면 advance_turn을 통해 다음 플레이어로 인덱스 변경.

Step 4: 특수 기능 (Pause & Confirmation)
- UI 교체 방식: Flet의 self.controls 리스트를 통째로 갈아끼우는 방식으로 일시정지 및 확인 창을 구현하여, 모달 팝업 시 발생할 수 있는 프리징 현상을 방지합니다.

---
### 4. 핵심 함수별 역할 ###
함수명,역할 설명
<img width="783" height="358" alt="image" src="https://github.com/user-attachments/assets/52aaefd6-141e-4d0f-8afe-37d81dbea9f0" />

---
### 5. UI/UX 디자인 포인트 ###
반응형 스코어보드: 현재 차례인 플레이어에게 하이라이트(녹색 테두리 및 👉 표시).

시각적 핀 배치: monospace 폰트와 이모지를 활용하여 삼각형 형태의 볼링 핀 배치 구현.

비동기 처리: 봇의 투구 시 threading을 활용하여 UI가 멈추지 않고 자연스럽게 다음 순서로 넘어가도록 설계.
<img width="994" height="922" alt="image" src="https://github.com/user-attachments/assets/940d7d01-6929-4189-a8a8-9efa2b954daf" />


